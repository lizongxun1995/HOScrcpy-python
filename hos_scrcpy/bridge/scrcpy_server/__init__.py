"""Bundled device-side so assets for the pure-Python stream bridge.

- libscrcpy_server*.z.so: video extension libraries (H.264 gRPC server)
- uitest_agent_*.so: touch agent libraries (uitest JSON socket)

Refresh with: python -m hos_scrcpy.bridge.extract_servers
"""
